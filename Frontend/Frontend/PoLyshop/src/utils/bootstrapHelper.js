import * as bootstrap from "bootstrap";

/**
 * Helper utility to provide Bootstrap in all components
 * This avoids having to import Bootstrap in every component that uses modals
 */
export default bootstrap;
